/** Semver. TOM watch no longer duplicates players; pick which event to pull. */
export const APP_VERSION = "1.2.10";
export const APP_CHANNEL = "beta";
export const APP_VERSION_LABEL = `v${APP_VERSION} ${APP_CHANNEL}`;
